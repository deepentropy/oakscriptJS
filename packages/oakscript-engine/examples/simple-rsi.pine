// Simple RSI Indicator
// This is an example PineScript file for testing the transpiler

indicator("Simple RSI", overlay=false)

// Calculate RSI
rsi_length = 14
rsi_value = ta.rsi(close, rsi_length)

// Plot
plot(rsi_value)
